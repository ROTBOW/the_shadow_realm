import React, {useEffect} from "react";
import { useHistory } from "react-router";
import './search.scss';

const Search = (props) => {
    const history = useHistory();

    return (
        <div>
            boi this is gonna be huge
        </div>
    )

}

export default Search;